from setuptools import setup, find_packages

setup(
    name='admin_levels_finder',
    version='1.0.0',
    description='Get admin levels based on latitude and longitude coordinates.',
    packages=find_packages(),
    include_package_data=True,
)
